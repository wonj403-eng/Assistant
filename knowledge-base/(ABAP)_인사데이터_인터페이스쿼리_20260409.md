# ABAP Masked Source
# 인사 I/F 쿼리
> 저장: 2026-04-09 13:00:37 / 마스킹 27건

```abap
        select a.empid LIFNR,

               a.empid STERM,

               a.name [BUKRS],

               a.firengname || ' ' || a.famengname [BUKRS],

               substr(a.deptcd,5,4)||decode(substr(a.bicd,1,1),'B','Y','A','A','D','M','G','Y','E','Y',substr(a.bicd,1,1))||'Z0' SORTL,

               a.nid [BUKRS],

               a.grpjoindt  GBDAT,

               a.jobgrdcd SCACD,

               nvl(substr(a.curzip,1,3)||substr(a.curzip,5,7),' ')  pstlz,

               SUBSTR(rtrim(ltrim(a.curzipaddr)),1,instr(rtrim(ltrim(a.curzipaddr)),' ')) [BUKRS],

               ltrim(SUBSTR(rtrim(ltrim(a.curzipaddr)),instr(rtrim(ltrim(a.curzipaddr)),' '))) [BUKRS],

               rtrim(ltrim(a.curdetaddr)) STRAS,

               '[BUKRS]' zwels,

               '[BUKRS]' land1,

               decode(a.bicd,'A','[[BUKRS]]','B','[[BUKRS]]','D','[[BUKRS]]','[[BUKRS]]') [BUKRS],

               INCUMBCD GBORT,

               SCHLCARRCD PROFS,

               nvl((SELECT [BUKRS](APPNTDT) [BUKRS] HRI_ORD_MAS B

                  WHERE  B.COMPCD = '[BUKRS]'

                    [BUKRS] B.COMPCD = A.COMPCD

                    [BUKRS] B.[BUKRS] = A.[BUKRS]

                    [BUKRS] B.JOBGRDCD = A.JOBGRDCD),[BUKRS](A.PROGSDT, A.JOBGRDSTDDT)) PROGSDT,

               b.USER_EMAIL EMAIL

          from hri_emp_mas a, PORTAL.HTW_USER b

         where ( a.INCUMBCD like 'A%' or

                 a.INCUMBCD like 'R%' and a.retappntdt >= '[[BUKRS]]' )

          and a.EMPID = b.EMP_NO;
```
